# ASP.NET Core Website

## Build & Run
```bash
cd /home/artookis/csharp-site
dotnet run --urls http://0.0.0.0:5000
```

Then open `http://localhost:5000` to verify, or access from another device via `<pi-ip>:5000`.